<%= form_for(sub_category) do |f| %>
  <% if sub_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sub_category.errors.count, "error") %> prohibited this sub_category from being saved:</h2>

      <ul>
      <% sub_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="field">
     <%= f.collection_select(:category_id, Category.all, :id, :cat_name, 
    { prompt: 'Select a category' }, { id: 'category-select' }) %>
  
 </div>
 <p></p>
  <div class="field">
    <%= f.label :sub_cat_name %>
    <%= f.text_field :sub_cat_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
