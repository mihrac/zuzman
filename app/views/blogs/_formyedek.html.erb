
<% if blog.user == current_user %>

<%= form_for(blog) do |f| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :btitle %>
    <%= f.text_field :btitle %>
  </div>

  <div class="field">
    <%= f.label :btext %>
    <%= f.text_area :btext %>
  </div>

  <div class="field">
    <%= f.label :category_id %>
    <%= select_tag "category_id", options_from_collection_for_select(Category.all, "id", "cat_name") %>
  </div>

  <div class="field">
    <%= f.label :sub_category_id %>
    <%= select_tag "category_id", options_from_collection_for_select(SubCategory.all, "id", "sub_cat_name") %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<% else %>
<p class="bg-danger">You don't have permission to editing this Question!</p>
<% end %>
